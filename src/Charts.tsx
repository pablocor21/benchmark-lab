import { ResponsiveContainer, RadarChart, PolarGrid, PolarAngleAxis, PolarRadiusAxis, Radar, Legend, BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ReferenceLine } from 'recharts'
import type {AnalysisRow,Scenario} from './data'
export function Charts({rows,scenario,type}:{rows:AnalysisRow[];scenario:Scenario;type:string}){
 const data=rows.map(r=>({name:r.id==='ux'?'UX':r.id==='service'?'Servicio':r.id==='personal'?'Personaliz.':r.name,Proyecto:r.values[3],Referente:r.best,Brecha:r.gap,[scenario.competitors[0]]:r.values[0],[scenario.competitors[1]]:r.values[1],[scenario.competitors[2]]:r.values[2]}))
 return <><div className="chart" role="img" aria-label={type==='radar'?'Radar de puntuaciones. Los datos completos están en la tabla inferior.':type==='bars'?'Barras de comparación por criterio. Los datos completos están en la tabla inferior.':'Brechas respecto al mejor competidor por criterio. Los datos completos están en la tabla inferior.'}><ResponsiveContainer width="100%" height="100%">{type==='radar'?<RadarChart data={data} outerRadius="58%"><PolarGrid stroke="#c8ccbc"/><PolarAngleAxis dataKey="name" tick={{fontSize:13,fill:'#363a2e'}}/><PolarRadiusAxis domain={[0,10]} tickCount={6} tick={{fontSize:11}}/>{scenario.competitors.map((c,i)=><Radar key={c} dataKey={c} stroke={['#596c4c','#397d87','#737078'][i]} fill="transparent" strokeDasharray={`${i+2} ${i+2}`} isAnimationActive={false}/>)}<Radar name={scenario.project} dataKey="Proyecto" stroke="#b7431e" fill="#b7431e" fillOpacity={.16} strokeWidth={3} isAnimationActive={false}/><Legend/></RadarChart>:<BarChart data={data} layout="vertical" margin={{left:10,right:30,top:20,bottom:10}}><CartesianGrid strokeDasharray="3 3" horizontal={false}/><XAxis type="number" domain={type==='gap'?[-9,9]:[0,10]}/><YAxis type="category" dataKey="name" width={135} tick={{fontSize:12,fill:'#34372d'}}/><Tooltip/><ReferenceLine x={0} stroke="#292d22"/>{type==='gap'?<Bar dataKey="Brecha" fill="#b7431e" isAnimationActive={false}/>:<><Bar name={scenario.project} dataKey="Proyecto" fill="#b7431e" isAnimationActive={false}/><Bar name="Mejor referente por criterio" dataKey="Referente" fill="#596c4c" isAnimationActive={false}/></>}<Legend/></BarChart>}</ResponsiveContainer></div><details className="chart-data"><summary>Ver datos del gráfico en tabla</summary><div className="table-scroll"><table><caption>Puntuaciones simuladas de 1 a 10; más alto es mejor.</caption><thead><tr><th>Criterio</th>{scenario.competitors.map(c=><th key={c}>{c}</th>)}<th>{scenario.project}</th><th>Brecha</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><th scope="row">{r.name}</th>{r.values.map((v,i)=><td key={i}>{v}</td>)}<td>{r.gap>0?'+':''}{r.gap}</td></tr>)}</tbody></table></div></details></>
}


