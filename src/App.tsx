import { lazy, Suspense, useEffect, useRef, useState } from 'react'
import { ArrowRight, ArrowLeft, ArrowUpRight, FlaskConical, BookOpen, X, Check, ChevronRight, SlidersHorizontal, BarChart3, Table2, Target, Monitor, Download, RotateCcw, Info } from 'lucide-react'
import { criteria, scenarios, loadStudy, analyze } from './data'
import type { Study, CriterionId, AnalysisRow } from './data'
const Charts = lazy(() => import('./Charts').then(module => ({ default: module.Charts })))
import { Matrix } from './Matrix'
import './App.css'
import './modules.css'
import Challenge from './challenge/Challenge'
import { ModuleNavigation } from './components/ModuleNavigation'
import { ModuleBridge } from './components/ModuleBridge'
const phases=['Definir','Comparar','Analizar','Identificar','Adaptar','Mejorar']
const titles=['','El punto de partida.','Elige qué importa.','Competitive matrix.','De los datos al criterio.','Adaptar, no copiar.','Benchmark Report.']
function App(){
 const [challengeOpen,setChallengeOpen]=useState(location.pathname==='/challenge')
 const [classMode,setClassMode]=useState(new URLSearchParams(location.search).has('presentacion'))
 function switchModule(module:'studio'|'challenge',teaching=false){setGuide(false);setPresent(false);setChallengeOpen(module==='challenge');setClassMode(teaching);history.pushState({},'',module==='challenge'?'/challenge'+(teaching?'?presentacion=1':''):'/');window.scrollTo(0,0)}
 const [study,setStudy]=useState<Study>(loadStudy)
 const [intro,setIntro]=useState(true)
 const [guide,setGuide]=useState(false)
 const [tab,setTab]=useState('matrix')
 const [chart,setChart]=useState('radar')
 const [detail,setDetail]=useState<CriterionId|null>(null)
 const [present,setPresent]=useState(location.pathname==='/presentacion')
 useEffect(()=>{document.title=challengeOpen?'Benchmark Lab — Benchmark Challenge':present?'Benchmark Lab — Presentación':'Benchmark Lab — Benchmark Studio'},[challengeOpen,present])
 const [slide,setSlide]=useState(0)
 const [storageError,setStorageError]=useState(false)
 const heading=useRef<HTMLHeadingElement>(null)
 const drawer=useRef<HTMLDivElement>(null)
 const firstRender=useRef(true)
 const scenario=scenarios.find(s=>s.id===study.scenarioId)!
 const rows=analyze(study)
 const sorted=[...rows].sort((a,b)=>a.gap-b.gap)
 const strengths=rows.filter(r=>r.gap>=0)
 const weaknesses=sorted.filter(r=>r.gap<0)
 const selectedPractice=rows.find(r=>r.id===study.practice)||sorted[0]
 const step=study.step||1
 const update=(patch:Partial<Study>)=>setStudy(s=>({...s,...patch}))
 useEffect(()=>{try{localStorage.setItem('benchmark-lab-v1',JSON.stringify(study));setStorageError(false)}catch{setStorageError(true)}},[study])
 useEffect(()=>{if(firstRender.current){firstRender.current=false;return}heading.current?.focus();window.scrollTo(0,0)},[study.step,intro,present,slide])
 useEffect(()=>{if(!guide)return;const previous=document.activeElement as HTMLElement;const panel=drawer.current!;panel.querySelector<HTMLButtonElement>('button')?.focus();const oldOverflow=document.body.style.overflow;document.body.style.overflow='hidden';const key=(e:KeyboardEvent)=>{if(e.key==='Escape')setGuide(false);if(e.key==='Tab'){const items=panel.querySelectorAll<HTMLElement>('button,a,[tabindex="0"]');const first=items[0],last=items[items.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}}};document.addEventListener('keydown',key);return()=>{document.removeEventListener('keydown',key);document.body.style.overflow=oldOverflow;previous?.focus()}},[guide])
 useEffect(()=>{const pop=()=>{setPresent(location.pathname==='/presentacion');setChallengeOpen(location.pathname==='/challenge');setClassMode(new URLSearchParams(location.search).has('presentacion'))};window.addEventListener('popstate',pop);return()=>window.removeEventListener('popstate',pop)},[])
 useEffect(()=>{if(!present||guide)return;const key=(e:KeyboardEvent)=>{if(e.key==='ArrowRight'){e.preventDefault();setSlide(s=>Math.min(6,s+1))}if(e.key==='ArrowLeft'){e.preventDefault();setSlide(s=>Math.max(0,s-1))}if(e.key==='Escape'){history.pushState({},'','/');setPresent(false);setIntro(false)}};window.addEventListener('keydown',key);return()=>window.removeEventListener('keydown',key)},[present,guide])
 function go(n:number){update({step:n});setIntro(false)}
 function startPresentation(){setSlide(0);setPresent(true);history.pushState({},'','/presentacion')}
 function choose(id:string){if(id===study.scenarioId)return;const s=scenarios.find(x=>x.id===id)!;update({scenarioId:id,scores:s.scores.map(r=>[...r]),adaptation:'',target:'',practice:'speed'})}
 function toggle(id:CriterionId){const included=study.selected.includes(id);if(included&&study.selected.length>4)update({selected:study.selected.filter(x=>x!==id)});if(!included&&study.selected.length<6)update({selected:[...study.selected,id]})}
 function edit(row:number,column:number,value:number){if(!Number.isInteger(value)||value<1||value>10)return;setStudy(s=>({...s,scores:s.scores.map((r,i)=>i===row?r.map((v,j)=>j===column?value:v):r)}))}
 function exportReport(){const content=['BENCHMARK LAB · BENCHMARK REPORT',`Proyecto: ${scenario.project}`,`Objetivo: ${scenario.objective}`,'Datos ficticios y puntuaciones educativas; no son evidencia de mercado.',`Referentes: ${scenario.competitors.join(', ')}`,'Escala 1–10: mayor puntuación = mejor desempeño; brecha = proyecto − mejor competidor.','',...rows.map(r=>`${r.name}: ${r.values.join(' / ')} | Referente: ${r.leaders.map(i=>scenario.competitors[i]).join(', ')} (${r.best}) | Brecha: ${r.gap}`),'','FORTALEZAS',...strengths.map(r=>`${r.name}: ${r.gap===0?'al nivel del referente':`${r.gap} ${r.gap===1?'punto':'puntos'} por encima`}`),...(strengths.length?[]:['Aún no hay criterios al nivel o por encima del mejor referente.']),'','BRECHAS',...weaknesses.map(r=>`${r.name}: ${-r.gap} ${-r.gap===1?'punto':'puntos'} por debajo`),...(weaknesses.length?[]:['No hay brechas negativas en los criterios seleccionados.']),'','BUENAS PRÁCTICAS Y OPORTUNIDADES',...rows.map(r=>`${r.name}: ${r.practice} Oportunidad: ${r.action}`),'','ADAPTACIÓN',selectedPractice.practice,study.adaptation,`Indicador y meta: ${study.target}`,'','CONCLUSIÓN','El benchmarking transforma referencias en decisiones adaptadas al contexto. Validar las acciones con datos reales antes de invertir.'].join('\n');const url=URL.createObjectURL(new Blob([content],{type:'text/plain;charset=utf-8'}));const a=document.createElement('a');a.href=url;a.download=`benchmark-${scenario.project.toLowerCase().replaceAll(' ','-')}.txt`;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000)}
 const gapLabel=(r:AnalysisRow)=>r.gap<0?`${-r.gap} por debajo`:r.gap>0?`${r.gap} por encima`:'Al nivel'
 const matrix=(editable=true)=><Matrix rows={rows} scenario={scenario} editable={editable} onEdit={edit} onExplain={id=>setDetail(detail===id?null:id)}/>
 const practiceList=<div className="research-list">{sorted.slice(0,3).map(r=><article key={r.id}><span className="index">0{sorted.indexOf(r)+1}</span><div><span className="eyebrow">{r.name} / {r.leaders.map(i=>scenario.competitors[i]).join(' + ')}</span><h3>{r.practice}</h3><p>{r.action}</p></div></article>)}</div>
 const slides=[{title:'¿Qué es benchmarking?',content:<><p className="presentation-statement">Observar referentes.<br/>Aprender de sus prácticas.<br/><em>Mejorar con criterio propio.</em></p><p>Un proceso sistemático de comparación para identificar buenas prácticas y oportunidades de mejora.</p></>},{title:'Qué analizamos',content:<><p className="presentation-statement">{scenario.project}</p><p>{scenario.objective}</p><div className="presentation-note">Referentes: {scenario.competitors.join(' · ')}<br/>{rows.length} criterios · Escala 1–10 · Datos simulados</div></>},{title:'Comparación',content:matrix(false)},{title:'Brechas',content:<div className="slide-gaps">{sorted.map(r=><div key={r.id}><strong>{r.gap>0?'+':''}{r.gap}</strong><span>{r.name}<small>{gapLabel(r)}</small></span></div>)}</div>},{title:'Mejores prácticas',content:practiceList},{title:'Recomendaciones',content:<><p className="presentation-note">Práctica elegida: {selectedPractice.practice}</p><p className="presentation-statement smaller">{study.adaptation||'Completa la adaptación en el estudio para definir una recomendación propia.'}</p><p><b>Cómo verificar la mejora:</b> {study.target||'Definir un indicador, una meta y un plazo.'}</p></>},{title:'Conclusión',content:<><p className="presentation-statement">Benchmarking<br/><em>≠ copiar.</em></p><p>Comparar permite reconocer diferencias. Adaptar convierte ese aprendizaje en una mejora pertinente para {scenario.project}.</p><p className="presentation-note">Siguiente paso: probar la adaptación y medir su resultado. Estos datos simulados no sustituyen la investigación de mercado.</p></>}]
 return <><a className="skip" href="#content">Saltar al contenido</a><header><button className="brand" onClick={()=>{switchModule('studio');setIntro(true)}} aria-label="Benchmark Lab, inicio"><FlaskConical size={24}/> BENCHMARK <b>LAB</b></button><ModuleNavigation active={challengeOpen?'challenge':'studio'} onNavigate={switchModule}/><button onClick={()=>setGuide(true)}><BookOpen size={17}/> Guía de campo</button></header>
 {storageError&&<p role="status" className="notice">El navegador no permite guardar el avance. Puedes continuar y descargar tu informe al terminar.</p>}
 {challengeOpen?<Challenge onStudio={()=>switchModule('studio')} presentation={classMode}/>:present?<main id="content" className="presentation"><div className="eyebrow">BENCHMARK STUDIO / RESUMEN PARA EXPOSICIÓN <span>{slide+1} / 7</span></div><h1 ref={heading} tabIndex={-1}>{slides[slide].title}</h1><div className="slide-content">{slides[slide].content}{slide===6&&<ModuleBridge presentation onPlay={()=>switchModule('challenge',true)}/>}</div><div className="presentation-controls"><button onClick={()=>setSlide(v=>Math.max(0,v-1))} disabled={slide===0}><ArrowLeft/> Anterior</button><button onClick={()=>{history.pushState({},'','/');setPresent(false);setIntro(false)}}>Volver al estudio</button><button className="primary" onClick={()=>setSlide(v=>Math.min(6,v+1))} disabled={slide===6}>Siguiente <ArrowRight/></button></div><p className="small">También puedes usar las flechas del teclado. Esc vuelve al estudio.</p></main>:intro?<main id="content" className="cover"><div className="eyebrow">LABORATORIO DE ESTRATEGIA / MÓDULO 01</div><div className="cover-grid"><section><h1 ref={heading} tabIndex={-1}>Observar mejor.<br/>Decidir <em>mejor.</em></h1><p className="lead">Aprende benchmarking comparando,<br/>analizando y tomando decisiones.</p><button className="primary" onClick={()=>go(study.step||1)}>{study.step?'Continuar análisis':'Iniciar análisis'} <ArrowRight size={18}/></button><button className="text-button" onClick={()=>setGuide(true)}>¿Qué es benchmarking? <ArrowUpRight size={16}/></button><p className="small">4 escenarios · Datos simulados · Tu propio criterio</p><p className="definition">El benchmarking es un proceso sistemático que permite comparar productos, procesos o empresas para identificar buenas prácticas y oportunidades de mejora.</p></section><section className="lab-sheet"><div className="sheet-top">BENCHMARK STUDIO <ArrowUpRight/></div><div className="big-index">01<span>/06</span></div><h2>De la observación<br/>a la mejora.</h2>{['Comparar','Detectar diferencias','Encontrar buenas prácticas','Adaptarlas','Mejorar'].map((x,i)=><div className="process" key={x}><span>0{i+1}</span>{x}<ArrowRight size={16}/></div>)}<div className="sheet-bottom">OBSERVA. APRENDE. ADAPTA.</div></section></div><footer><span>Un referente es un punto de partida.<br/><b>No un destino para copiar.</b></span><span>DEFINIR → COMPARAR → ANALIZAR → IDENTIFICAR → ADAPTAR → MEJORAR</span></footer></main>:<>
 <nav className="phase-nav" aria-label="Fases del análisis">{phases.map((p,i)=>{const active=(step<=2?0:step===3?(tab==='matrix'?1:2):step-1)===i;return <button key={p} aria-current={active?'step':undefined} className={active?'active':''} disabled={(i>0&&step===1)||(i===5&&(study.adaptation.trim().length<20||study.target.trim().length<10))} onClick={()=>{if(i===2)setTab('charts');if(i===1)setTab('matrix');go(i===0?1:i===1||i===2?3:i+1)}}><span>0{i+1}</span>{p}<ChevronRight size={14}/></button>})}</nav>
 <main id="content" className="studio" key={step}><div className="workspace-meta"><span>BENCHMARK STUDIO</span><span>ESTUDIO / {scenario.project.toUpperCase()} <span className="saved">{storageError?'SIN GUARDADO':'GUARDADO LOCAL'}</span></span></div><div className="section-heading"><div><div className="eyebrow">{step===1?'ETAPA 1 · DEFINIR EL OBJETIVO':step===2?'ETAPA 2 · DEFINIR CÓMO MEDIR':step===3?'ETAPA 3 · COMPARAR Y ANALIZAR':step===4?'ETAPA 4 · IDENTIFICAR BRECHAS':step===5?'ETAPA 5 · ADAPTAR':'ETAPA 6 · MEJORAR'}</div><h1 ref={heading} tabIndex={-1}>{titles[step]}</h1></div><span className="page-number">0{step}<small>/06</small></span></div>
 {step===1&&<><p className="intro-text">Toda comparación empieza con una pregunta. Elige un proyecto y entiende qué necesita mejorar.</p><div className="dossier-layout"><div className="dossier-list" role="group" aria-label="Escenario de investigación">{scenarios.map((s,i)=><button key={s.id} aria-pressed={s.id===study.scenarioId} className={s.id===study.scenarioId?'selected':''} onClick={()=>choose(s.id)}><span className="index">0{i+1}</span><span><small>{s.tag}</small><strong>{s.name}</strong><span>{s.project}</span></span>{s.id===study.scenarioId?<ArrowUpRight/>:<ArrowRight size={18}/>}</button>)}</div><section className="dossier"><div className="sheet-top">DOSSIER DEL PROYECTO <span>DATOS FICTICIOS</span></div><h2>{scenario.project}</h2><p>{scenario.context}</p><dl><dt>Objetivo</dt><dd>{scenario.objective}</dd><dt>Problema</dt><dd>{scenario.problem}</dd><dt>Mercado</dt><dd>{scenario.market}</dd></dl><div className="reference-strip"><span>REFERENTES</span>{scenario.competitors.map(c=><b key={c}>{c}</b>)}</div></section></div><div className="action-bar"><span><Info size={17}/> Cambiar de escenario restablece sus puntuaciones y adaptación.</span><button className="primary" onClick={()=>go(2)}>Definir criterios <ArrowRight size={18}/></button></div></>}
 {step===2&&<><div className="split-intro"><p className="intro-text">Selecciona entre 4 y 6 criterios. Comparar las mismas dimensiones permite encontrar diferencias útiles para el objetivo de {scenario.project}.</p><div className="selection-count"><b>{study.selected.length}</b><span>/ 6 criterios<br/>Mínimo 4</span></div></div><div className="criteria-grid">{criteria.map((c,i)=>{const selected=study.selected.includes(c.id);const locked=selected?study.selected.length===4:study.selected.length===6;return <button key={c.id} className={selected?'selected':''} aria-pressed={selected} aria-disabled={locked} onClick={()=>toggle(c.id)}><div><span className="index">0{i+1}</span><span className="checkbox">{selected&&<Check size={16}/>}</span></div><h3>{c.name}</h3><p>{c.description}</p></button>})}</div><p role="status" className="small">{study.selected.length===6?'Límite alcanzado: desmarca un criterio para elegir otro.':study.selected.length===4?'Mínimo alcanzado: añade un criterio antes de quitar otro.':'Puedes añadir o quitar un criterio.'}</p><div className="action-bar"><button onClick={()=>go(1)}><ArrowLeft size={16}/> Escenario</button><button className="primary" onClick={()=>{setTab('matrix');go(3)}}>Construir comparación <ArrowRight size={18}/></button></div></>}
 {step===3&&<><div className="matrix-top"><p className="intro-text">Las diferencias son el inicio de la investigación.<br/>Edita las puntuaciones y observa cómo cambia el diagnóstico.</p><div className="tabs" role="tablist" aria-label="Vista del análisis">{[{id:'matrix',name:'Comparación',icon:Table2},{id:'charts',name:'Gráficos',icon:BarChart3}].map(t=><button id={`tab-${t.id}`} aria-controls="analysis-panel" role="tab" aria-selected={tab===t.id} tabIndex={tab===t.id?0:-1} onKeyDown={e=>{if(['ArrowRight','ArrowLeft'].includes(e.key)){e.preventDefault();const next=tab==='matrix'?'charts':'matrix';setTab(next);document.getElementById(`tab-${next}`)?.focus()}}} className={tab===t.id?'selected':''} key={t.id} onClick={()=>setTab(t.id)}><t.icon size={16}/>{t.name}</button>)}</div></div><div id="analysis-panel" role="tabpanel" aria-labelledby={`tab-${tab}`}>{tab==='matrix'?<>{matrix()}{detail&&<aside className="criterion-detail"><Info size={18}/><p><b>{criteria.find(c=>c.id===detail)?.name}.</b> {criteria.find(c=>c.id===detail)?.description}</p><button aria-label="Cerrar explicación" onClick={()=>setDetail(null)}><X size={18}/></button></aside>}<div className="matrix-legend"><span><i/> REF. = mayor puntuación entre competidores; se muestran todos los empates.</span><span><SlidersHorizontal size={15}/> Puntuaciones editables de 1 a 10</span></div></>:<><div className="chart-toolbar"><label htmlFor="chart-type">Visualización</label><select id="chart-type" value={chart} onChange={e=>setChart(e.target.value)}><option value="radar">Radar · perfil competitivo</option><option value="bars">Barras · comparación por criterio</option><option value="gap">Brechas · distancia al referente</option></select></div><Suspense fallback={<p role="status">Preparando visualización…</p>}><Charts rows={rows} scenario={scenario} type={chart}/></Suspense></>}</div><div className="analysis-summary"><div><span>PRINCIPAL BRECHA</span><b>{weaknesses.length?weaknesses[0].name:'Sin brechas negativas'}</b></div><strong>{weaknesses.length?weaknesses[0].gap:0}<small>pts</small></strong><p>{weaknesses.length?`Distancia respecto a ${weaknesses[0].leaders.map(i=>scenario.competitors[i]).join(' y ')}. Investiga el proceso que produce esta diferencia.`:'El proyecto iguala o supera las referencias en los criterios elegidos. Valida esta hipótesis con evidencia.'}</p></div><div className="action-bar"><button onClick={()=>go(2)}><ArrowLeft size={16}/> Ajustar criterios</button><button className="primary" onClick={()=>go(4)}>Descubrir insights <ArrowRight size={18}/></button></div></>}
 {step===4&&<><p className="intro-text">Una puntuación señala dónde mirar. La investigación explica por qué ocurre y qué podemos aprender.</p><div className="findings"><section><span className="index">01 / FORTALEZAS</span><h2>Lo que ya funciona.</h2>{strengths.length?strengths.map(r=><p key={r.id}><b>{r.name}.</b> {scenario.project} está {r.gap===0?'al nivel del mejor referente':`${r.gap} ${r.gap===1?'punto':'puntos'} por encima del mejor referente`} ({r.values[3]}/10).</p>):<p>Ningún criterio iguala todavía al mejor referente. El desempeño más cercano está en <b>{sorted[sorted.length-1].name}</b>.</p>}</section><section><span className="index">02 / BRECHAS</span><h2>Dónde enfocar el esfuerzo.</h2>{weaknesses.length?weaknesses.map(r=><p key={r.id}><b>{r.name} · {r.gap} pts.</b> {scenario.project} está {-r.gap} {-r.gap===1?'punto':'puntos'} por debajo de {r.leaders.map(i=>scenario.competitors[i]).join(' y ')}.</p>):<p>No hay brechas negativas. Mantén el desempeño y explora nuevas referencias.</p>}</section></div><div className="research-title"><span className="index">03 / MEJORES PRÁCTICAS Y OPORTUNIDADES</span><span>HIPÓTESIS DIDÁCTICAS PARA INVESTIGAR</span></div>{practiceList}<aside className="education-note"><BookOpen size={20}/><p><b>Una buena práctica no se deduce solo de una cifra.</b> Estas prácticas son hipótesis del escenario ficticio. En un proyecto real, entrevistarías a usuarios y estudiarías procesos para verificarlas.</p></aside><div className="action-bar"><button onClick={()=>go(3)}><ArrowLeft size={16}/> Comparación</button><button className="primary" onClick={()=>{update({practice:sorted[0].id});go(5)}}>Convertir en acciones <ArrowRight size={18}/></button></div></>}
 {step===5&&<><div className="adapt-layout"><section className="manifesto"><span className="eyebrow">EL PRINCIPIO DEL LABORATORIO</span><h2>BENCHMARKING<br/><em>≠ COPIAR</em></h2><div>{['Observar','Comparar','Aprender','Adaptar','Mejorar'].map((p,i)=><p key={p}><span>{i===0?'=':'+'}</span>{p}</p>)}</div><p className="manifesto-caption">Aprender del referente, respetando el contexto y los recursos de tu proyecto.</p></section><section className="adapt-form"><label htmlFor="practice">01 / Buena práctica encontrada</label><select id="practice" value={selectedPractice.id} onChange={e=>update({practice:e.target.value as CriterionId})}>{rows.map(r=><option key={r.id} value={r.id}>{r.name} · {r.leaders.map(i=>scenario.competitors[i]).join(' / ')}</option>)}</select><div className="practice-note"><Target size={22}/><p>{selectedPractice.practice}</p></div><label htmlFor="adaptation">02 / ¿Cómo la adaptarías a {scenario.project}?</label><p className="field-hint" id="adapt-hint">Describe un cambio concreto y viable con tus recursos. Mínimo 20 caracteres.</p><textarea id="adaptation" aria-describedby="adapt-hint" value={study.adaptation} maxLength={1200} placeholder={`En ${scenario.project}, podríamos…`} onChange={e=>update({adaptation:e.target.value})}/><label htmlFor="target">03 / ¿Cómo sabrás si mejoró?</label><p className="field-hint" id="target-hint">Incluye un indicador, una meta y un plazo. Ejemplo: reducir el tiempo de atención de 15 a 10 minutos en 4 semanas.</p><textarea id="target" className="target-input" aria-describedby="target-hint" value={study.target} maxLength={600} placeholder="Indicador + meta + plazo" onChange={e=>update({target:e.target.value})}/></section></div><div className="action-bar"><button onClick={()=>go(4)}><ArrowLeft size={16}/> Insights</button><button className="primary" disabled={study.adaptation.trim().length<20||study.target.trim().length<10} onClick={()=>go(6)}>Generar Benchmark Report <ArrowRight size={18}/></button></div></>}
 {step===6&&<><div className="report-banner"><div><span className="eyebrow">PROYECTO ANALIZADO</span><h2>{scenario.project}</h2><p>{scenario.objective}</p></div><button className="primary" onClick={startPresentation}><Monitor size={18}/> Ver resumen para exposición</button></div><div className="report-facts"><div><span>REFERENTES</span><p>{scenario.competitors.join(' · ')}</p></div><div><span>CRITERIOS</span><p>{rows.map(r=>r.name).join(' · ')}</p></div></div><div className="findings"><section><span className="index">FORTALEZAS</span>{strengths.length?strengths.map(r=><p key={r.id}><b>{r.name}:</b> {gapLabel(r)} del mejor referente.</p>):<p>Aún no se iguala al mejor referente en ningún criterio.</p>}</section><section><span className="index">DEBILIDADES / BRECHAS</span>{weaknesses.length?weaknesses.map(r=><p key={r.id}><b>{r.name}:</b> {gapLabel(r)} del mejor referente.</p>):<p>No hay brechas negativas en esta selección.</p>}</section></div><section className="report-benchmark"><span className="index">BENCHMARK IDENTIFICADO</span><h2>{selectedPractice.leaders.map(i=>scenario.competitors[i]).join(' y ')} <span>· {selectedPractice.best}/10 en {selectedPractice.name.toLowerCase()}</span></h2><p>{selectedPractice.practice}</p><p className="field-hint">Referencia por criterio; no implica que una empresa sea la mejor en todas las dimensiones.</p></section><div className="research-title"><span className="index">MEJORES PRÁCTICAS / OPORTUNIDADES</span></div>{practiceList}<section className="recommended"><span className="eyebrow">ACCIÓN RECOMENDADA / TU ADAPTACIÓN</span><h2>{study.adaptation||'Pendiente de definir: vuelve a Adaptar para completar tu propuesta.'}</h2><p><b>Indicador y meta:</b> {study.target||'Pendiente de definir.'}</p><p>Realiza un piloto, mide el resultado y ajusta la acción antes de ampliar su alcance.</p></section><aside className="education-note"><Info size={20}/><p>Informe educativo con empresas ficticias. Las puntuaciones y prácticas simuladas sirven para aprender el método; las decisiones reales requieren fuentes verificables.</p></aside><div className="action-bar"><button onClick={()=>go(5)}><ArrowLeft size={16}/> Revisar adaptación</button><button onClick={exportReport}><Download size={17}/> Descargar informe .txt</button></div></>}
 <footer className="studio-footer"><span>BENCHMARK LAB <b>/</b> Observar. Aprender. Adaptar.</span><button onClick={()=>setIntro(true)}><RotateCcw size={14}/> Volver al laboratorio</button></footer></main></>}
 {!challengeOpen&&!present&&<div className="lab-module-bridge"><ModuleBridge onPlay={()=>switchModule('challenge')}/></div>}
 {guide&&<div className="drawer-backdrop" onMouseDown={e=>{if(e.target===e.currentTarget)setGuide(false)}}><div className="drawer" ref={drawer} role="dialog" aria-modal="true" aria-labelledby="guide-title"><div className="sheet-top">GUÍA DE CAMPO <button onClick={()=>setGuide(false)} aria-label="Cerrar guía"><X size={20}/></button></div><h2 id="guide-title">Aprender del entorno.<br/>Mejorar lo propio.</h2><p>El benchmarking es un proceso sistemático que compara productos, procesos o empresas para identificar buenas prácticas y oportunidades de mejora.</p><div className="guide-callout">Benchmarking ≠ copiar</div>{phases.map((p,i)=><section key={p}><span className="index">0{i+1} / {p.toUpperCase()}</span><p>{['Define el objetivo, el problema y los referentes relevantes.','Elige criterios comunes y mide con una escala consistente.','Observa la diferencia entre tu proyecto y el mejor referente por criterio.','Investiga qué prácticas podrían explicar las diferencias.','Ajusta una práctica a tus recursos, público y contexto.','Prueba la adaptación y mide si lograste el resultado esperado.'][i]}</p></section>)}<h3>Cómo leer el estudio</h3><p>Todos los valores van de 1 a 10. Más alto siempre significa mejor desempeño. En precio, significa mayor asequibilidad, no mayor costo.</p><p><b>Brecha = proyecto − mejor competidor.</b> Un resultado negativo indica distancia por cerrar; cero, igualdad; positivo, ventaja.</p><p className="small">Empresas, puntuaciones y prácticas son ficticias. No representan una investigación de mercado real.</p></div></div>}
 </>
}
export default App





